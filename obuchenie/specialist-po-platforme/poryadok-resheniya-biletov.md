---
order: 0.8
title: Порядок решения билетов
---

## Подготовка конфигурации

1. Включить управляемые блокировки

2. Включить использование обычных форм для консоли запросов

3. Добавить Подсистемы

4. Включить в настройках подсветку Идентификаторов и Автозамену с подсказкой

5. Вид-Настройка панелей-панель открытых

### Решение Оперативный учет

1. Включить разделение итогов

2. Для измерений обязательно заполнение

3. Настроить справочники (например Номенклатура Услуга

4. Настроить Регистры сведений (например учетную политику)

5. Настроить Реквизиты документов Приходная/Расходная накладные ?

6. Реализовать проведение ОУ в приходной накладной. Не забыть условие по ссылке в запросе и Движения.Записывать = Истина. в настройках формы-показывать движения

7. Реализовать проведение ОУ в расходной накладной 7.1 Очистить движение документа 7.2 Установить блокировку при старом методе и БлокироватьДляИзменения при новом. 7.3 Проверка isnull в запросе. 7.4 Проверить связи в запросах 7.5 Граница включая для нового метола, МоментВремени для старого 7.6 В запросе получать Представление 7.7 Не забывать про проверку на услугу 7.8. Сортировка по Методу списания. 7.9 Не забывать индексировать поля виртуальных таблиц по которым будем потом поиск!! 8.Обязательно выводить Представление номенклатуры для вывода в ошибке.

8. Как быстро набрать текст управляемой блокировки:

9. Вводим в модуле текст "БлокировкаДанных"

10. Выделяем, Ctrl+F1, открывается синтаксис-помочник

11. Выделяем пример , мышкой перемещаем текст в модуль ( Drag&Drop)

12. Переименовуем регистр, удаляем лишние строки и т.д.

13. PROFIT Все делается за 30 сек.

### Решение Бухгалтерский учет

1. Завессти необходимые сущности (справочники/реквизиты док-ов и т.д.)

2. Создать док операция (обязательный запрет проведения) Перед записью:

   ```
   Проводки = Движения.Управленческий;	 
   Если НЕ Проводки.Модифицированность() И НЕ ЭтоНовый() тогда 
   	Проводки.Прочитать(); 
   КонецЕсли;	 
   Проводки.УстановитьАктивность(НЕ ПометкаУдаления);	 
   Для каждого Строка Из Проводки Цикл 
   	Строка.Период = Дата; 
   КонецЦикла;
   ```

3. //\*\*\*\*\*\*\*\*\*\*\*\*Еще вариант

```
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	Проводки = Движения.Управленческий;
	Проводки.Записывать = Истина;
	
	Если Не Проводки.Модифицированность() И Не ЭтоНовый() Тогда
		Проводки.Прочитать();
	КонецЕсли;
	
	Для каждого Движение Из Проводки Цикл
		Движение.Период = Дата;
	КонецЦикла;
	
	Если Не ЭтоНовый() И ПометкаУдаления <> Ссылка.ПометкаУдаления Тогда
		Проводки.УстановитьАктивность(Не ПометкаУдаления);
	КонецЕсли;
КонецПроцедуры

Процедура ПриКопировании(ОбъектКопирования)
	ОбъектКопирования.Движения.Управленческий.Прочитать();
	Для каждого ИсхЗапись Из ОбъектКопирования.Движения.Управленческий Цикл
		Движение = Движения.Управленческий.Добавить();
		ЗаполнитьЗначенияСвойств(Движение, ИсхЗапись);
	КонецЦикла;
КонецПроцедуры
```

/\*\*\*\*\*\*\*\*\*\*\*\*\*

1. Настроить ПВХ Виды субконто

2. Настроить План Счетов

3. Настроить Регистр Бухгалтерии

   1. Связь с планов счетов + Корреспонденция

   2. Измерения (обязательность заполнения, балансовые, признаки учета)

   3. Ресурсы (обязательность заполнения, балансовые, признаки учета)

**ПВХ**

1. Спр подчиняем ПВХ

2. ПВХ тип значения и доп свойства - спр значения свойств

3. РС Свойства Объектов:

   1. Измерение Владелец - тип спр Номенклатура

   2. Измерение Свойство - тип ПВХ

   3. Ресурс Значение - тип Характеристика.Значение. Связи параметров выбора и связь по типу с измерением Свойство.